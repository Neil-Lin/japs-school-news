const esc=s=>(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const articleTarget=a=>(a.summary||'').trim()?a.url:(a.siteUrl||a.sourceUrl||a.url);const articleLabel=a=>(a.summary||'').trim()?'閱讀公告':'此篇只有附檔 請前往官網查看';const articleAttachments=a=>(a.attachments||[]).filter(Boolean).map(item=>{const name=typeof item==='string'?item:item.name;return item.url?`<a class="attachment" href="${esc(item.url)}" target="_blank" rel="noopener">附件：${esc(name)}</a>`:`<span class="attachment">附件：${esc(name)}</span>`}).join('');
const fmt=v=>v?new Intl.DateTimeFormat('zh-TW',{dateStyle:'medium',timeStyle:'short',timeZone:'Asia/Taipei'}).format(new Date(v)):'時間未提供';
const mergeArticles=articles=>{const merged=new Map();articles.forEach(article=>{const key=article.title.replace(/^(轉知[:：]\s*)+/,'')+'::'+(article.schools||['仁愛國小']).slice().sort().join('|')+'::'+(article.source||'');const current=merged.get(key);if(!current){merged.set(key,{...article,schools:[...(article.schools||['仁愛國小'])],sources:[...(article.sources||[article.source]).filter(Boolean)]});return}current.schools=[...new Set([...current.schools,...(article.schools||[])])];current.sources=[...new Set([...current.sources,...(article.sources||[article.source]).filter(Boolean)])];if((article.summary||'').length>(current.summary||'').length)current.summary=article.summary;if(article.published>current.published)current.published=article.published});return [...merged.values()]};
const schoolSlugs={仁愛國小:'renai',建安國小:'jianan',敦化國小:'dunhua',博愛國小:'boai',光復國小:'guangfu'};
const activeSchool=Object.entries(schoolSlugs).find(([,slug])=>new RegExp('/'+slug+'(?:/|$)').test(location.pathname))?.[0]||'仁愛國小';
fetch('data/articles.json?v='+Date.now()).then(r=>r.json()).then(data=>{document.querySelector('#updated').textContent=data.updatedAt?'資料更新｜'+fmt(data.updatedAt):'尚未更新';const schools=['仁愛國小','建安國小','敦化國小','博愛國小','光復國小'];const articles=mergeArticles(data.articles);const switcher=document.querySelector('#schoolSwitch');const render=()=>{switcher.innerHTML=schools.map(s=>`<button type="button" class="school-tab ${s===activeSchool?'active':''}" aria-selected="${s===activeSchool}" data-school="${s}">${s}</button>`).join('');document.querySelectorAll('.school-tab').forEach(b=>b.onclick=()=>{activeSchool=b.dataset.school;render()});document.querySelector('#schoolTitle').innerHTML=activeSchool+'<br><em>消息牆</em>';const visible=articles.filter(a=>(a.schools||['仁愛國小']).includes(activeSchool));const groups=[...new Set(visible.flatMap(a=>a.sources||[a.source]))];let active='全部';document.querySelector('#filters').innerHTML=['全部',...groups].map(g=>`<button type="button" class="filter ${g===active?'active':''}" data-source="${esc(g)}">${esc(g)}</button>`).join('');const paint=()=>{document.querySelectorAll('.filter').forEach(b=>b.classList.toggle('active',b.dataset.source===active));const list=visible.filter(a=>active==='全部'||(a.sources||[a.source]).includes(active));document.querySelector('#feed').innerHTML=list.length?list.map(a=>`<article class="card"><div class="meta"><span class="badges">${(a.sources||[a.source]).map(source=>`<span class="tag">${esc(source)}</span>`).join('')}</span><time>${esc(fmt(a.published))}</time></div><h2>${esc(a.title)}</h2>${articleTarget(a)?`<a class="read" href="${esc(articleTarget(a))}" target="_blank" rel="noopener">${articleLabel(a)} <span>↗</span></a>`:`<p class="summary">${esc(a.summary||'沒有可顯示的內容。')}</p>`}${articleAttachments(a)}</article>`).join(''):'<p class="empty">目前沒有可顯示的消息。</p>'};document.querySelectorAll('.filter').forEach(b=>b.onclick=()=>{active=b.dataset.source;paint()});paint()};render()}).catch(()=>document.querySelector('#feed').innerHTML='<p class="empty">暫時無法載入消息資料，請稍後再試。</p>');
